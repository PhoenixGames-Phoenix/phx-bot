name: Prettier CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Check prettier code style
        run: npx prettier --config ./.prettierrc.yaml -c ./src/
